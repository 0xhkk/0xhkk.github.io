---
layout: post
title: "一文搞懂 SPL Token"
---
# 一文搞懂 SPL Token

---

## 30 秒速览

```
┌───────────────────────────────────────────────────────────────────────┐
│                                                                       │
│     🏭 Mint              📦 Token Account         🪪 Metadata         │
│     代币的模板            你的钱包                  代币的身份证        │
│     ─────────            ────────                 ────────────        │
│     "USDC 是什么"         "你有多少 USDC"          "USDC 叫什么名字"    │
│                                                                       │
│     82 字节               165 字节                 ~679 字节           │
│     每种代币 1 个          每人每币 1 个            每种代币 1 个        │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

**三句话总结：**
1. **Mint** = 代币的"出生证明"，定义精度和总量
2. **Token Account** = 用户的"代币钱包"，记录持有数量
3. **Metadata** = 代币的"名片"，存储名称、图片等信息

---

## 一、Mint：代币的模板

### 是什么？

> **类比：Mint 就像「印钞厂的模板」**
> 
> 它定义了：这是什么币？精度多少？谁能印？——但它本身不存钱。

**一句话：** Mint 是代币的定义，每种代币有且只有一个 Mint。

### 核心字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `decimals` | u8 | 小数位数（USDC=6，SOL=9） |
| `supply` | u64 | 当前总供应量 |
| `mint_authority` | Option | 谁能铸造新币（None=固定供应） |
| `freeze_authority` | Option | 谁能冻结账户 |

### 代码骨架

```rust
pub struct Mint {
    pub mint_authority: COption<Pubkey>,   // 铸币权限
    pub supply: u64,                        // 总供应量
    pub decimals: u8,                       // 小数位数
    pub is_initialized: bool,               // 是否已初始化
    pub freeze_authority: COption<Pubkey>, // 冻结权限
}
// 总大小：82 字节
```

### `is_initialized` 状态说明

Mint 账户的 `is_initialized` 是一个**防止重复初始化**的标志：

| 值 | 状态 | 说明 |
|----|------|------|
| `false` | 未初始化 | 账户刚创建，还没调用 `InitializeMint` |
| `true` | 已初始化 | 正常可用状态，不能再次初始化 |

```
账户生命周期：
  
  创建账户          调用 InitializeMint          正常使用
      │                    │                        │
      ▼                    ▼                        ▼
┌──────────┐         ┌──────────┐            ┌──────────┐
│ 空账户   │ ──────► │ is_init  │ ─────────► │ 可铸造   │
│ (0 bytes)│         │ = true   │            │ 可转账   │
└──────────┘         └──────────┘            └──────────┘
```

> **安全意义**：防止攻击者对已存在的 Mint 重新初始化，篡改 `mint_authority`。

### 常见问题

<details>
<summary><b>Q: decimals = 9 是什么意思？</b></summary>

表示 1 个代币 = 10^9 个最小单位。

```
用户看到：1.5 SOL
链上存储：1_500_000_000（15 亿个 lamport）
```

这样设计的好处是：链上只存整数，避免浮点数精度问题。
</details>

<details>
<summary><b>Q: 如何创建固定供应量的代币？</b></summary>

铸造完成后，将 `mint_authority` 设为 `None`：

```rust
mint.mint_authority = COption::None;  // 再也无法铸造新币
```
</details>

> 💡 **关于 NFT**：NFT 也使用 SPL Token 系统，但参数和流程不同。
> 详见 [1.1.nft-deep-dive.md](./1.1.nft-deep-dive.md)

---

## 二、Token Account：你的钱包

### 是什么？

> **类比：Token Account 就像「银行账户」**
> 
> Mint 是"美元的定义"，Token Account 是"你在某银行开的美元账户"。

**一句话：** Token Account 存储某人持有某种代币的数量。

### 核心字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `mint` | Pubkey | 这是什么代币 |
| `owner` | Pubkey | 谁拥有这个账户 |
| `amount` | u64 | 余额 |
| `state` | enum | 正常/冻结/未初始化 |
| `delegate` | Option | 授权给谁操作 |

### 代码骨架

```rust
pub struct Account {
    pub mint: Pubkey,                      // 关联的 Mint
    pub owner: Pubkey,                     // 所有者
    pub amount: u64,                       // 余额
    pub delegate: COption<Pubkey>,         // 代理
    pub state: AccountState,               // 状态（见下方说明）
    pub is_native: COption<u64>,           // 是否是 Wrapped SOL
    pub delegated_amount: u64,             // 代理额度
    pub close_authority: COption<Pubkey>,  // 关闭权限
}
// 总大小：165 字节
```

### `AccountState` 状态说明

Token Account 的 `state` 字段有**三种状态**：

```rust
pub enum AccountState {
    Uninitialized = 0,  // 未初始化
    Initialized = 1,    // 正常可用
    Frozen = 2,         // 已冻结
}
```

| 值 | 状态 | 说明 | 能做什么 |
|----|------|------|---------|
| `0` | **Uninitialized** | 账户刚创建，未初始化 | ❌ 不能操作 |
| `1` | **Initialized** | 正常可用状态 | ✅ 转账、接收、关闭 |
| `2` | **Frozen** | 被 `freeze_authority` 冻结 | ❌ 不能转账，但能接收 |

```
Token Account 生命周期：

  创建账户       InitializeAccount        正常使用           FreezeAccount
      │                │                     │                    │
      ▼                ▼                     ▼                    ▼
┌─────────────┐  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│Uninitialized│─►│ Initialized │ ◄──► │   转账中    │ ───► │   Frozen    │
│   (0)       │  │    (1)      │      │   接收中    │      │    (2)      │
└─────────────┘  └─────────────┘      └─────────────┘      └──────┬──────┘
                                                                  │
                                                           ThawAccount
                                                                  │
                                                                  ▼
                                                           回到 Initialized
```

> **Frozen 状态的用途**：
> - 合规场景：冻结可疑账户（如 USDC 的发行方可以冻结违规地址）
> - 安全场景：发现被盗后立即冻结，防止资产转移

### ATA：你最常用的 Token Account

**ATA（Associated Token Account）** 是一种特殊的 Token Account，地址可以根据公式计算出来：

```
ATA 地址 = PDA([钱包地址, Token程序ID, Mint地址], Associated Token Program ID)
```

**好处：** 不需要告诉别人你的 Token Account 地址，他们可以自己算出来！

```typescript
// 任何人都能算出你的 USDC 账户地址
const yourUsdcAccount = getAssociatedTokenAddress(
  USDC_MINT,        // Mint 地址
  yourWallet        // 你的钱包地址
);
```

### 常见问题

<details>
<summary><b>Q: 为什么要有 Token Account？不能直接在钱包里存代币吗？</b></summary>

Solana 的设计理念是「账户分离」：

- **钱包地址**：只是一个身份（公钥）
- **Token Account**：真正存代币的地方

好处是：每个代币账户可以有独立的权限控制。
</details>

<details>
<summary><b>Q: Wrapped SOL 是什么？</b></summary>

原生 SOL 不是 SPL Token，但有时需要把它当 Token 用（比如在 DEX 交易）。

Wrapped SOL 就是把 SOL 存入一个 Token Account：
- 存入 1 SOL → 账户 lamports +1 SOL，amount +1
- 取出时反向操作

```
Native SOL Mint: So11111111111111111111111111111111111111112
```
</details>

<details>
<summary><b>Q: delegate 有什么用？</b></summary>

允许别人代你操作一定额度的代币，常见于：
- DEX 授权：允许 DEX 扣你最多 100 USDC
- 订阅服务：允许服务商每月扣款

```rust
// 授权后
account.delegate = Some(dex_address);
account.delegated_amount = 100_000_000;  // 100 USDC
```
</details>

---

## 三、Metadata：代币的身份证

### 是什么？

> **类比：Metadata 就像「商品标签」**
> 
> Mint 只是一串代码，Metadata 告诉你它叫什么名字、长什么样。

**一句话：** Metadata 存储代币的名称、符号、图片等人类可读信息。

### 核心字段（FT 常用）

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | String | 代币名称（最长 32 字符） |
| `symbol` | String | 代币符号（最长 10 字符） |
| `uri` | String | 指向 JSON 的链接（logo 等） |
| `update_authority` | Pubkey | 谁能修改 metadata |

### 代码骨架

```rust
pub struct Metadata {
    pub key: Key,                              // 账户类型标识（MetadataV1）
    pub update_authority: Pubkey,              // 更新权限
    pub mint: Pubkey,                          // 关联的 Mint 地址
    pub data: Data,                            // 核心数据（name, symbol, uri）
    pub primary_sale_happened: bool,           // 是否已首次销售
    pub is_mutable: bool,                      // 是否可修改
    pub edition_nonce: Option<u8>,             // NFT 用：Edition PDA bump
    pub token_standard: Option<TokenStandard>, // Fungible / NonFungible 等
    pub collection: Option<Collection>,        // NFT 用：所属集合
    pub uses: Option<Uses>,                    // NFT 用：使用次数
    pub collection_details: Option<CollectionDetails>, // NFT 用：集合详情
    pub programmable_config: Option<ProgrammableConfig>, // pNFT 用
}

/// 核心数据
pub struct Data {
    pub name: String,                          // 名称（最长 32 字符）
    pub symbol: String,                        // 符号（最长 10 字符）
    pub uri: String,                           // JSON 元数据链接（最长 200 字符）
    pub seller_fee_basis_points: u16,          // 版税（FT 通常为 0）
    pub creators: Option<Vec<Creator>>,        // 创作者列表（FT 通常为 None）
}
```

> 💡 **NFT 专属字段**（`edition_nonce`, `collection`, `uses` 等）对于普通 FT 都是 `None`。
> 详见 [1.1.nft-deep-dive.md](./1.1.nft-deep-dive.md)

### Metadata 地址是 PDA

```
Metadata 地址 = PDA(["metadata", 程序ID, Mint地址])
```

```typescript
const [metadataPDA] = PublicKey.findProgramAddressSync(
  [
    Buffer.from("metadata"),
    METADATA_PROGRAM_ID.toBuffer(),
    mintAddress.toBuffer(),
  ],
  METADATA_PROGRAM_ID
);
```

### 常见问题

<details>
<summary><b>Q: 图片存在哪里？</b></summary>

图片**不存在链上**！`uri` 字段指向一个 JSON 文件，JSON 里有图片链接：

```
链上 Metadata
     │
     └─► uri: "https://arweave.net/xxx"
              │
              └─► JSON: { "image": "https://arweave.net/yyy.png" }
                              │
                              └─► 实际图片文件
```

常见存储服务：Arweave、IPFS、AWS S3
</details>

<details>
<summary><b>Q: seller_fee_basis_points 是什么？</b></summary>

二级市场版税，单位是「基点」（主要用于 NFT）：
- 100 = 1%
- 500 = 5%
- 1000 = 10%

**普通 FT 通常设为 0**（没有版税）。
</details>

<details>
<summary><b>Q: 普通 FT 需要 Metadata 吗？</b></summary>

**可选，但推荐**。有 Metadata 后钱包才能显示代币的名字和 logo。

没有 Metadata 的代币在钱包里只显示地址，用户体验差。
</details>

---

## 四、三者的关系

### 4.1 ATA 地址是怎么算出来的？

> **核心概念**：ATA（Associated Token Account）的地址不是随机生成的，
> 而是由 **3 个输入** 通过 PDA 公式**确定性计算**出来的。

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│    ATA 地址 = findProgramAddress(seeds, Associated Token Program)  │
│                                                                     │
│    ┌─────────────────────────────────────────────────────────────┐  │
│    │                        seeds（3 个输入）                     │  │
│    ├─────────────────────────────────────────────────────────────┤  │
│    │                                                             │  │
│    │   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐      │  │
│    │   │   Wallet    │ + │Token Program│ + │    Mint     │      │  │
│    │   │   (Alice)   │   │     ID      │   │   (USDC)    │      │  │
│    │   │  5yNj...    │   │ TokenkegQf..│   │  EPjFWdd5.. │      │  │
│    │   └──────┬──────┘   └──────┬──────┘   └──────┬──────┘      │  │
│    │          │                 │                 │              │  │
│    └──────────┼─────────────────┼─────────────────┼──────────────┘  │
│               │                 │                 │                 │
│               └────────────────►│◄────────────────┘                 │
│                                 │                                   │
│                                 ▼                                   │
│                    ┌────────────────────────┐                       │
│                    │    PDA 派生算法        │                       │
│                    │  findProgramAddress() │                       │
│                    └───────────┬────────────┘                       │
│                                │                                    │
│                                ▼                                    │
│                    ┌────────────────────────┐                       │
│                    │   ATA 地址（确定的）    │                       │
│                    │    DShz4m...           │                       │
│                    │    ← 任何人都能算出来   │                       │
│                    └────────────────────────┘                       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**关键理解**：
- 同一个 Wallet + 同一个 Mint = **永远得到同一个 ATA 地址**
- 不同 Token Program（SPL Token vs Token-2022）= **不同的 ATA 地址**
- ATA 地址是**计算出来的**，不是链上查询的

### 4.2 完整关系图

```
图例：  ┄┄┄► 虚线 = 种子（用于 PDA 派生）
        ───► 实线 = 指向关系（字段引用）

┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                              │
│   👥 用户区域                        🏭 代币定义                    📋 元数据区域             │
│                                                                                              │
│   ┌─────────────┐                   ┌─────────────┐               ┌─────────────────┐       │
│   │ 🧑 Alice    │                   │             │               │    Metadata     │       │
│   │   Wallet    │                   │    Mint     │               │  (~679 bytes)   │       │
│   │  (Keypair)  │                   │  (82 bytes) │               │                 │       │
│   └──────┬──────┘                   │             │               │  name: "USDC"   │       │
│          ┆                          │ supply: 💰  │               │  symbol: "USDC" │       │
│          ┆ + Mint                   │ decimals: 6 │               │  uri ─────────────────► │
│          ┆ + TOKEN_PROGRAM_ID       │             │               │  creators: [...] │      │
│          ┆                          └──────┬──────┘               └────────▲────────┘       │
│          ▼ (PDA 派生)                      │                               ┆                │
│   ┌─────────────┐                          │                               ┆ PDA 派生       │
│   │Token Account│                          │                               ┆ seeds=[        │
│   │   (ATA)     │                          │                               ┆ "metadata",    │
│   │  165 bytes  │                          │                               ┆ METADATA_      │
│   │             │                          │                               ┆ PROGRAM_ID,    │
│   │ owner: Alice│                          │                               ┆ mint_address   │
│   │ amount: 300 │──── mint 字段 ──────────►│◄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┘ ]              │
│   └─────────────┘                          │                                                │
│                                            │                                                │
│   ┌─────────────┐                          │               ┌─────────────────────────┐      │
│   │ 🧑 Bob      │                          │               │      链下存储            │◄─────┤
│   │   Wallet    │                          │               │                         │      │
│   │  (Keypair)  │                          │               │  JSON (Arweave / IPFS)  │      │
│   └──────┬──────┘                          │               │   ├─ name               │      │
│          ┆                                 │               │   ├─ description        │      │
│          ┆ + Mint                          │               │   ├─ image ───────► 🖼️   │      │
│          ┆ + TOKEN_PROGRAM_ID              │               │   └─ attributes         │      │
│          ▼ (PDA 派生)                      │               └─────────────────────────┘      │
│   ┌─────────────┐                          │                                                │
│   │Token Account│                          │                                                │
│   │   (ATA)     │                          │                                                │
│   │  165 bytes  │                          │                                                │
│   │             │                          │                                                │
│   │ owner: Bob  │──── mint 字段 ──────────►│                                                │
│   │ amount: 700 │                          │                                                │
│   └─────────────┘                          │                                                │
│                                            │                                                │
│            📊 余额校验：Alice(300) + Bob(700) = supply(1000) ✓                              │
│                                                                                              │
└──────────────────────────────────────────────────────────────────────────────────────────────┘

派生关系一览：

  ATA 派生（Associated Token Program 计算）：
  ┌────────────────────────────────────────────────────────────────────┐
  │                                                                    │
  │   Wallet 地址 ┄┄┄┄┄┄┄┄┐                                            │
  │                       ┆                                            │
  │   TOKEN_PROGRAM_ID ┄┄┄┼┄┄┄► PDA 派生 ───► ATA 地址                 │
  │                       ┆                                            │
  │   Mint 地址 ┄┄┄┄┄┄┄┄┄┄┘                                            │
  │                                                                    │
  └────────────────────────────────────────────────────────────────────┘

  Metadata 派生（Metaplex Metadata Program 计算）：
  ┌────────────────────────────────────────────────────────────────────┐
  │                                                                    │
  │   "metadata" ┄┄┄┄┄┄┄┄┄┄┄┐                                          │
  │                         ┆                                          │
  │   METADATA_PROGRAM_ID ┄┄┼┄┄┄► PDA 派生 ───► Metadata 地址          │
  │                         ┆                                          │
  │   Mint 地址 ┄┄┄┄┄┄┄┄┄┄┄┄┘                                          │
  │                                                                    │
  └────────────────────────────────────────────────────────────────────┘
```

### 4.3 关系总结

| 关系 | 通俗解释 |
|------|---------|
| **Wallet → ATA** | 钱包 + 代币类型 → 算出你的代币账户地址 |
| **ATA → Mint** | 每个 ATA 都知道"我存的是什么币" |
| **Mint → Metadata** | 代币地址 → 算出它的名片在哪 |
| **Metadata → JSON** | 名片里有个链接，指向图片和详细信息 |

**一句话总结**：
> ATA 地址 = **钱包 + 代币类型** 的唯一组合，任何人都能算出来，不用查链。
    
• Metadata ──uri字段──► JSON ──► 图片
    图片存储在链下（Arweave/IPFS）
```

---

## 五、实战：创建你的第一个代币

### 完整流程（5 步）

```typescript
import {
  Connection,
  Keypair,
  PublicKey,
  SystemProgram,
  Transaction,
  sendAndConfirmTransaction,
  clusterApiUrl,
  LAMPORTS_PER_SOL,
} from "@solana/web3.js";
import * as fs from "fs";
import * as path from "path";
import {
  TOKEN_PROGRAM_ID,
  createInitializeMintInstruction,
  createAssociatedTokenAccountInstruction,
  createMintToInstruction,
  getAssociatedTokenAddress,
  MINT_SIZE,
} from "@solana/spl-token";
import {
  PROGRAM_ID as METADATA_PROGRAM_ID,
  createCreateMetadataAccountV3Instruction,
} from "@metaplex-foundation/mpl-token-metadata";

// ═══════════════════════════════════════════════════════════════
// 配置区域 - 根据需要修改
// ═══════════════════════════════════════════════════════════════

// 使用 devnet（测试网）
const NETWORK = "devnet";

// 代币配置
const TOKEN_CONFIG = {
  name: "My Token",
  symbol: "MTK",
  decimals: 9,
  uri: "https://example.com/token.json", // 可以替换为你的 JSON 元数据 URL
  mintAmount: 1_000_000_000, // 1 token (因为 decimals = 9)
};

// ═══════════════════════════════════════════════════════════════
// 主函数
// ═══════════════════════════════════════════════════════════════

async function main() {
  console.log("🚀 开始创建代币...\n");

  // 1. 建立连接
  const connection = new Connection(clusterApiUrl(NETWORK), "confirmed");
  console.log(`📡 已连接到 ${NETWORK}`);

  // 2. 加载或创建钱包
  let payer: Keypair;
  
  // 尝试从本地钱包文件加载
  // 使用 __dirname 获取当前脚本所在目录
  const localWalletPath = path.join(__dirname, "test.json");
  
  if (fs.existsSync(localWalletPath)) {
    // 使用本地已有钱包（推荐，通常已有 SOL）
    const secretKey = JSON.parse(fs.readFileSync(localWalletPath, "utf-8"));
    payer = Keypair.fromSecretKey(Uint8Array.from(secretKey));
    console.log(`👛 使用本地钱包: ${payer.publicKey.toBase58()}`);
    console.log(`   (来自 ${localWalletPath})`);
  } else {
    // 生成新的临时钱包（需要空投）
    payer = Keypair.generate();
    console.log(`👛 生成新钱包: ${payer.publicKey.toBase58()}`);
    console.log(`🔑 钱包私钥: [${payer.secretKey.toString()}]`);
    console.log(`   (请保存私钥！)`);
  }


  // 检查余额
  const balance = await connection.getBalance(payer.publicKey);
  console.log(`💵 余额: ${balance / LAMPORTS_PER_SOL} SOL\n`);

  // 4. 创建代币
  await createToken(connection, payer);
}

async function createToken(connection: Connection, payer: Keypair) {
  // 使用同一个密钥作为 payer、authority 和 user（简化测试）
  const authority = payer;
  const user = payer;

  // ═══════════════════════════════════════════════════════════════
  // Step 1: 创建 Mint 账户
  // ═══════════════════════════════════════════════════════════════
  console.log("📝 Step 1: 创建 Mint 账户...");
  const mint = Keypair.generate();
  console.log(`   Mint 地址: ${mint.publicKey.toBase58()}`);
  console.log(`   Mint 私钥: [${mint.secretKey.toString()}]`);

  const lamports = await connection.getMinimumBalanceForRentExemption(MINT_SIZE);

  const createMintAccountIx = SystemProgram.createAccount({
    fromPubkey: payer.publicKey,
    newAccountPubkey: mint.publicKey,
    lamports,
    space: MINT_SIZE,
    programId: TOKEN_PROGRAM_ID,
  });

  // ═══════════════════════════════════════════════════════════════
  // Step 2: 初始化 Mint
  // ═══════════════════════════════════════════════════════════════
  console.log("📝 Step 2: 初始化 Mint...");
  const initMintIx = createInitializeMintInstruction(
    mint.publicKey,
    TOKEN_CONFIG.decimals,
    authority.publicKey,
    authority.publicKey // freeze authority (可选，设为 null 则无法冻结)
  );

  // ═══════════════════════════════════════════════════════════════
  // Step 3: 创建 Metadata
  // ═══════════════════════════════════════════════════════════════
  console.log("📝 Step 3: 创建 Metadata...");
  const [metadataPDA] = PublicKey.findProgramAddressSync(
    [
      Buffer.from("metadata"),
      METADATA_PROGRAM_ID.toBuffer(),
      mint.publicKey.toBuffer(),
    ],
    METADATA_PROGRAM_ID
  );
  console.log(`   Metadata PDA: ${metadataPDA.toBase58()}`);

  const createMetadataIx = createCreateMetadataAccountV3Instruction(
    {
      metadata: metadataPDA,
      mint: mint.publicKey,
      mintAuthority: authority.publicKey,
      payer: payer.publicKey,
      updateAuthority: authority.publicKey,
    },
    {
      createMetadataAccountArgsV3: {
        data: {
          name: TOKEN_CONFIG.name,
          symbol: TOKEN_CONFIG.symbol,
          uri: TOKEN_CONFIG.uri,
          sellerFeeBasisPoints: 0,
          creators: null,
          collection: null,
          uses: null,
        },
        isMutable: true,
        collectionDetails: null,
      },
    }
  );

  // ═══════════════════════════════════════════════════════════════
  // Step 4: 创建 Token Account (ATA)
  // ═══════════════════════════════════════════════════════════════
  console.log("📝 Step 4: 创建 Token Account (ATA)...");
  const ata = await getAssociatedTokenAddress(mint.publicKey, user.publicKey);
  console.log(`   ATA 地址: ${ata.toBase58()}`);

  const createAtaIx = createAssociatedTokenAccountInstruction(
    payer.publicKey,
    ata,
    user.publicKey,
    mint.publicKey
  );

  // ═══════════════════════════════════════════════════════════════
  // Step 5: 铸造代币
  // ═══════════════════════════════════════════════════════════════
  console.log("📝 Step 5: 铸造代币...");
  const mintToIx = createMintToInstruction(
    mint.publicKey,
    ata,
    authority.publicKey,
    TOKEN_CONFIG.mintAmount
  );

  // ═══════════════════════════════════════════════════════════════
  // 发送交易
  // ═══════════════════════════════════════════════════════════════
  console.log("\n📤 发送交易...");
  const tx = new Transaction().add(
    createMintAccountIx,
    initMintIx,
    createMetadataIx,
    createAtaIx,
    mintToIx
  );

  try {
    const signature = await sendAndConfirmTransaction(connection, tx, [
      payer,
      mint,
    ]);

    console.log("\n" + "═".repeat(60));
    console.log("✅ 代币创建成功!");
    console.log("═".repeat(60));
    console.log(`   代币名称: ${TOKEN_CONFIG.name}`);
    console.log(`   代币符号: ${TOKEN_CONFIG.symbol}`);
    console.log(`   小数位数: ${TOKEN_CONFIG.decimals}`);
    console.log(`   Mint 地址: ${mint.publicKey.toBase58()}`);
    console.log(`   Metadata: ${metadataPDA.toBase58()}`);
    console.log(`   你的 ATA: ${ata.toBase58()}`);
    console.log(`   铸造数量: ${TOKEN_CONFIG.mintAmount / 10 ** TOKEN_CONFIG.decimals} ${TOKEN_CONFIG.symbol}`);
    console.log("═".repeat(60));
    console.log(`\n🔗 在 Solana Explorer 查看:`);
    console.log(`   https://explorer.solana.com/address/${mint.publicKey.toBase58()}?cluster=${NETWORK}`);
    console.log(`\n📝 交易签名:`);
    console.log(`   https://explorer.solana.com/tx/${signature}?cluster=${NETWORK}`);
  } catch (error) {
    console.error("❌ 交易失败:", error);
    throw error;
  }
}

// 运行主函数
main().catch(console.error);

}
```

> 💡 **创建 NFT**：NFT 的创建流程更复杂，需要额外创建 Master Edition。
> 详见 [1.1.nft-deep-dive.md](./1.1.nft-deep-dive.md)

---

## 六、底层原理：客户端代码背后发生了什么？

上面的 TypeScript 代码调用了几个指令，但它们在链上实际做了什么？让我们结合 SPL Token 合约源码来看。

### 6.1 流程总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     创建代币的底层流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Step 1: CreateAccount              Step 2: InitializeMint                  │
│  ══════════════════════            ════════════════════════                 │
│  由 System Program 执行             由 SPL Token Program 执行                │
│                                                                             │
│  ┌─────────────────────┐           ┌─────────────────────┐                  │
│  │    分配 82 字节      │    ──►   │   填充 Mint 数据     │                  │
│  │    转入租金 SOL      │           │   设置权限和精度     │                  │
│  │    设置 owner =      │           │   标记已初始化       │                  │
│  │    Token Program     │           │                     │                  │
│  └─────────────────────┘           └─────────────────────┘                  │
│           │                                   │                              │
│           ▼                                   ▼                              │
│  ┌─────────────────────────────────────────────────────────┐                │
│  │                     Mint Account (82 bytes)             │                │
│  │  mint_authority | supply: 0 | decimals | is_initialized │                │
│  └─────────────────────────────────────────────────────────┘                │
│                                                                             │
│                              Step 3: MintTo                                 │
│                             ════════════════                                │
│                             由 SPL Token Program 执行                        │
│                                                                             │
│  ┌─────────────────────┐           ┌─────────────────────┐                  │
│  │    Mint Account     │           │   Token Account     │                  │
│  │    supply += 100    │           │   amount += 100     │                  │
│  └─────────────────────┘           └─────────────────────┘                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Step 1: CreateAccount（System Program）

**这一步不是 SPL Token 做的，是 System Program！**

```typescript
// 客户端代码
SystemProgram.createAccount({
  fromPubkey: payer.publicKey,      // 谁付钱
  newAccountPubkey: mint.publicKey, // 新账户地址
  lamports: rent,                   // 租金（约 0.00145 SOL）
  space: 82,                        // 分配 82 字节
  programId: TOKEN_PROGRAM_ID,      // owner 设为 Token Program
});
```

**底层做了什么：**

| 操作 | 说明 |
|------|------|
| 扣除 lamports | 从 `payer` 转移租金到新账户 |
| 分配空间 | 创建 82 字节的存储空间（初始全为 0） |
| 设置 owner | 将账户 owner 设为 Token Program |

```
创建后的账户状态：
┌──────────────────────────────────────┐
│ Address: mint.publicKey              │
│ Owner: Token Program                 │
│ Lamports: ~1,461,600 (租金)          │
│ Data: [0, 0, 0, 0, ... 共82字节全0]  │  ← 还没初始化
└──────────────────────────────────────┘
```

### 6.3 Step 2: InitializeMint（SPL Token Program）

```typescript
// 客户端代码
createInitializeMintInstruction(
  mint.publicKey,           // mint 地址
  9,                        // decimals
  authority.publicKey,      // mint authority
  authority.publicKey,      // freeze authority
);
```

**SPL Token 合约代码（精简版）：**

```rust
// 来源：spl-token/program/src/processor.rs

fn _process_initialize_mint(
    accounts: &[AccountInfo],
    decimals: u8,
    mint_authority: Pubkey,
    freeze_authority: COption<Pubkey>,
) -> ProgramResult {
    let mint_info = next_account_info(account_info_iter)?;
    
    // ① 读取账户数据（82字节的0）, 转换成 Mint 结构体
    let mut mint = Mint::unpack_unchecked(&mint_info.data.borrow())?;
    
    // ② 检查是否已经初始化过（防止重复初始化）
    if mint.is_initialized {
        return Err(TokenError::AlreadyInUse.into());
    }

    // ③ 检查租金是否足够
    if !rent.is_exempt(mint_info.lamports(), mint_data_len) {
        return Err(TokenError::NotRentExempt.into());
    }

    // ④ 填充 Mint 数据结构
    mint.mint_authority = COption::Some(mint_authority);  // 谁能铸币
    mint.decimals = decimals;                              // 精度
    mint.is_initialized = true;                            // 标记已初始化
    mint.freeze_authority = freeze_authority;              // 谁能冻结
    // supply 保持为 0

    // ⑤ 序列化并写回账户
    Mint::pack(mint, &mut mint_info.data.borrow_mut())?;

    Ok(())
}
```

**初始化后的内存变化：**

```
Before (全0):
[00 00 00 00 00 00 00 00 00 00 ... 00 00 00 00]

After (结构化数据):
┌─────────┬──────────────────────────────────────────┐
│ Offset  │ Data                                     │
├─────────┼──────────────────────────────────────────┤
│ 0-3     │ [01 00 00 00]              ← Some tag    │
│ 4-35    │ [mint_authority 公钥]                    │
│ 36-43   │ [00 00 00 00 00 00 00 00]  ← supply = 0  │
│ 44      │ [09]                       ← decimals=9  │
│ 45      │ [01]                       ← initialized │
│ 46-81   │ [freeze_authority...]                    │
└─────────┴──────────────────────────────────────────┘
```

### 6.4 Step 3: MintTo（SPL Token Program）

```typescript
// 客户端代码
createMintToInstruction(
  mint.publicKey,       // mint
  ata,                  // 目标 Token Account
  authority.publicKey,  // mint authority（必须签名）
  1_000_000_000         // 数量
);
```

**SPL Token 合约代码（精简版）：**

```rust
// 来源：spl-token/program/src/processor.rs

pub fn process_mint_to(
    program_id: &Pubkey,
    accounts: &[AccountInfo],
    amount: u64,
) -> ProgramResult {
    let mint_info = next_account_info(account_info_iter)?;
    let destination_account_info = next_account_info(account_info_iter)?;
    let owner_info = next_account_info(account_info_iter)?;  // mint_authority

    // ① 读取并验证 Token Account
    let mut destination_account = Account::unpack(&destination_account_info.data.borrow())?;
    
    if destination_account.is_frozen() {
        return Err(TokenError::AccountFrozen.into());
    }
    if !cmp_pubkeys(mint_info.key, &destination_account.mint) {
        return Err(TokenError::MintMismatch.into());  // 必须是同一种代币
    }

    // ② 读取 Mint 并验证权限
    let mut mint = Mint::unpack(&mint_info.data.borrow())?;
    
    match mint.mint_authority {
        COption::Some(mint_authority) => {
            // 验证签名者是否是 mint_authority
            Self::validate_owner(program_id, &mint_authority, owner_info, ...)?;
        },
        COption::None => {
            return Err(TokenError::FixedSupply.into());  // 固定供应量，不能铸币
        }
    }

    // ③ 核心操作：同时增加两个值
    destination_account.amount = destination_account.amount
        .checked_add(amount)
        .ok_or(TokenError::Overflow)?;

    mint.supply = mint.supply
        .checked_add(amount)
        .ok_or(TokenError::Overflow)?;

    // ④ 写回两个账户
    Account::pack(destination_account, &mut destination_account_info.data.borrow_mut())?;
    Mint::pack(mint, &mut mint_info.data.borrow_mut())?;

    Ok(())
}
```

**MintTo 前后对比：**

```
Before:                              After (mint 1_000_000_000):
┌─────────────────────┐              ┌─────────────────────┐
│    Mint Account     │              │    Mint Account     │
│    supply: 0        │      ──►     │    supply: 1B       │
└─────────────────────┘              └─────────────────────┘

┌─────────────────────┐              ┌─────────────────────┐
│   Token Account     │              │   Token Account     │
│    amount: 0        │      ──►     │    amount: 1B       │
└─────────────────────┘              └─────────────────────┘

📍 关键：两个值同步增加，保证 sum(所有amount) == supply
```

### 6.5 为什么分成 CreateAccount + InitializeMint 两步？

| 原因 | 说明 |
|------|------|
| **权限分离** | System Program 管空间分配，Token Program 管业务逻辑 |
| **灵活性** | 可以用不同方式创建账户（普通账户、PDA 等） |
| **安全性** | 创建时就锁定 owner，只有 Token Program 能修改数据 |

---

## 附录

### A. 完整数据结构（带字节偏移）

<details>
<summary><b>Mint Account 内存布局 (82 bytes)</b></summary>

```
┌────────┬────────────────────┬─────────┬───────────────────────────────────┐
│ Offset │     Field          │  Size   │           Description             │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│   0    │ mint_authority     │ 36 B    │ [tag: 4B] + [pubkey: 32B]         │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  36    │ supply             │  8 B    │ u64, 总供应量                      │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  44    │ decimals           │  1 B    │ u8, 小数位数                       │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  45    │ is_initialized     │  1 B    │ bool                              │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  46    │ freeze_authority   │ 36 B    │ [tag: 4B] + [pubkey: 32B]         │
└────────┴────────────────────┴─────────┴───────────────────────────────────┘
```
</details>

<details>
<summary><b>Token Account 内存布局 (165 bytes)</b></summary>

```
┌────────┬────────────────────┬─────────┬───────────────────────────────────┐
│ Offset │     Field          │  Size   │           Description             │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│   0    │ mint               │ 32 B    │ Pubkey                            │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  32    │ owner              │ 32 B    │ Pubkey                            │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  64    │ amount             │  8 B    │ u64                               │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│  72    │ delegate           │ 36 B    │ COption<Pubkey>                   │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│ 108    │ state              │  1 B    │ AccountState                      │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│ 109    │ is_native          │ 12 B    │ COption<u64>                      │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│ 121    │ delegated_amount   │  8 B    │ u64                               │
├────────┼────────────────────┼─────────┼───────────────────────────────────┤
│ 129    │ close_authority    │ 36 B    │ COption<Pubkey>                   │
└────────┴────────────────────┴─────────┴───────────────────────────────────┘
```
</details>

### B. COption 设计原理

<details>
<summary><b>为什么不用 Rust 的 Option？</b></summary>

Rust 的 `Option<T>` 大小不固定，而链上账户需要**固定大小**。

`COption` 是 C 风格的 Option，结构固定：

```rust
// COption<Pubkey> = 36 bytes
// ├─ tag:    4 bytes  (0 = None, 1 = Some)
// └─ value: 32 bytes  (Pubkey)

// 内存表示：
// None:  [0, 0, 0, 0] + [32 bytes 零值]
// Some:  [1, 0, 0, 0] + [32 bytes 实际公钥]
```

**为什么 tag 用 4 字节而不是 1 字节？**

为了内存对齐！4 字节边界对齐可以：
1. 提高 CPU 读取效率
2. 保证跨语言（C、Rust、JS）兼容
</details>

### C. 核心程序地址

```
SPL Token Program:      TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
Associated Token:       ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL
Token Metadata:         metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s
Native SOL Mint:        So11111111111111111111111111111111111111112
```

---

## 总结对照表

| 维度 | Mint | Token Account（ata）| Metadata |
|------|------|---------------|----------|
| **类比** | 印钞厂模板 | 银行账户 | 商品标签 |
| **回答的问题** | 这是什么币？ | 你有多少？ | 它叫什么？ |
| **大小** | 82 B | 165 B | ~679 B |
| **每种代币** | 1 个 | N 个（每人 1 个） | 1 个 |
| **管理程序** | SPL Token | SPL Token | Metaplex |
| **地址类型** | 随机 | 随机或 PDA (ATA) | PDA |

---

